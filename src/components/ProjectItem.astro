---
import type { CollectionEntry } from 'astro:content';

type Props = Pick<
  CollectionEntry<'projects'>['data'],
  'title' | 'description' | 'thumbnail'
> &
  Pick<CollectionEntry<'projects'>, 'slug'>;

const { title, description, thumbnail, slug } = Astro.props;
---

<a
  class='block'
  href={`/projects/${slug}`}
>
  <article class='space-y-2'>
    <figure>
      <img
        src={thumbnail.src}
        alt={thumbnail.alt}
        class='w-full h-64 object-cover'
      />
    </figure>

    <div>
      <h4 class='text-lg font-medium text-indigo-500'>{title}</h4>
      <p class='text-sm text-gray-500'>
        {description}
      </p>
    </div>
  </article>
</a>
